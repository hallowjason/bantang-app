rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 活動圖片：任何人可讀（班員入口公開顯示）；已登入者可上傳
    match /event-images/{imageId} {
      allow read:  if true;
      allow write: if request.auth != null;
    }
  }
}
